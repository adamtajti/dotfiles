return {
	"tzachar/highlight-undo.nvim",
	-- event = "VeryLazy",
	lazy = false,
	enabled = false, -- disabled because it's buggy atm, but it's under development, it may work well later on
	config = true,
	opts = {},
}
