#!/usr/bin/env bash

# Configure Git
# TODO: Ensure that git is installed to run this command
git config --global hub.protocol ssh # prefer ssh
git config --global core.pager 'less -F' # don't use pager for single screen
